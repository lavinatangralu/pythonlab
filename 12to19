PYTHON OPERATORS
12. Program to Find Maximum of Three Numbers

a = float(input("Enter first number: "))
b = float(input("Enter second number: "))
c = float(input("Enter third number: "))

if a >= b and a >= c:
    maximum = a
elif b >= a and b >= c:
    maximum = b
else:
    maximum = c

print(f"The maximum number is {maximum}")

13. Program to Check if a Year is Leap

year = int(input("Enter a year: "))

if (year % 400 == 0) or (year % 4 == 0 and year % 100 != 0):
    print(f"{year} is a leap year")
else:
    print(f"{year} is not a leap year")

14. Program to Check if a Date is Valid

import datetime

day = int(input("Enter day: "))
month = int(input("Enter month: "))
year = int(input("Enter year: "))

try:
    date = datetime.date(year, month, day)
    print("The date is valid:", date)
except ValueError:
    print("The date is invalid")

15. Program to Find Roots of a Quadratic Equation

import math

a = float(input("Enter coefficient a: "))
b = float(input("Enter coefficient b: "))
c = float(input("Enter coefficient c: "))

D = b**2 - 4*a*c  # Discriminant

if D > 0:
    root1 = (-b + math.sqrt(D)) / (2 * a)
    root2 = (-b - math.sqrt(D)) / (2 * a)
    print(f"Two real roots: {root1:.2f} and {root2:.2f}")
elif D == 0:
    root = -b / (2 * a)
    print(f"One real root: {root:.2f}")
else:
    real = -b / (2 * a)
    imag = math.sqrt(-D) / (2 * a)
    print(f"Complex roots: {real:.2f} ± {imag:.2f}i")

16. Program to Delete Characters Whose Indices Are Divisible by 3

s = input("Enter a string: ")
result = ""

for i in range(len(s)):
    if i % 3 != 0:
        result += s[i]

print("Resulting string:", result)

17. Program to Find Length of Widest Fragment with Equal Elements

print("Enter sequence of integers ending with 0:")

prev = int(input())
max_len = 1
curr_len = 1

while True:
    num = int(input())
    if num == 0:
        break
    if num == prev:
        curr_len += 1
    else:
        max_len = max(max_len, curr_len)
        curr_len = 1
    prev = num

max_len = max(max_len, curr_len)
print("Length of the widest fragment:", max_len)

18. Program to Simulate Bowling Pins

N = int(input("Enter number of pins: "))
K = int(input("Enter number of balls: "))

pins = ["I"] * N  # All pins standing

for _ in range(K):
    l, r = map(int, input("Enter start and end pin numbers: ").split())
    for i in range(l - 1, r):
        pins[i] = "."  # Knock down pins in range

print("".join(pins))

19. Program to Find Seconds Between Two Timestamps
print("Enter first timestamp (hours minutes seconds):")
h1, m1, s1 = map(int, input().split())

print("Enter second timestamp (hours minutes seconds):")
h2, m2, s2 = map(int, input().split())
time1 = h1 * 3600 + m1 * 60 + s1
time2 = h2 * 3600 + m2 * 60 + s2
difference = time2 - time1
print("Seconds between timestamps:", difference)

